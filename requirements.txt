Flask==2.3.3
flask-babel==3.1.0
python-dotenv==1.0.0
requests==2.31.0
joblib==1.3.2
Pillow==10.1.0
gunicorn==20.1.0

# numeric: pin numpy before pandas so pip picks compatible wheels
numpy==1.26.4
pandas==2.2.2

# If you need PyTorch, add it later with a Docker deployment
# torch==2.2.0
# torchvision==0.16.0
